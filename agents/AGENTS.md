# paper-agent — agent instructions

You are working in the **paper-agent** repository.

## Scope

This project is intentionally minimal. Prefer small, focused changes. Do not add large frameworks or dependencies unless an issue explicitly requires them.

## Conventions

- Keep agent-specific instructions and profiles under `agents/` (see `agents/README.md`).
- Match existing style in any file you touch.
- When completing work tracked in an external task system, summarize what changed and how to verify it.

## Repository layout

- `agents/` — agent instructions, optional `cursor/` notes for Cursor rules, and per-role subfolders.
- `README.md` — high-level project pointer.
